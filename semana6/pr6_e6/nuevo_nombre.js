/**
    * Función para que el usuario introduzca su nombre
    * @type {string} nombre introducido
    * @returns {string} nombre 
*/
function nuevo_nombre() {
    var nombre = prompt("Dime el nombre")
    return nombre
}