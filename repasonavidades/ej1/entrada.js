/**
* Función para añadir un dato supuestamente relevante a un array
* @returns { array } arraycinho - Array con el dato actualizado
*/
function entrada() {
    var entrada = prompt("Explique el teorema de Pitágoras")
    return entrada
}

