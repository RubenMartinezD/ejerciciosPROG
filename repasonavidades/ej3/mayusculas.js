/**
 * Función para convertir un string a mayusculas
 * @param {string} cadena - string de texto origen 
 */
function mayusculear(cadena) {
    var resultado = cadena.toUpperCase()
    return resultado
}